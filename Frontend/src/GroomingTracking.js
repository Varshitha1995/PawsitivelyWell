import React from "react";

const GroomingTracking = ({dogName}) => {
    return (
        <div>
            <h3>Grooming Tracking div</h3>
            <h5>{dogName}</h5>
        </div>
    );
}

export default GroomingTracking;